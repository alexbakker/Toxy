<UserControl x:Class="Toxy.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:Toxy.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance ViewModels:ProfileViewModel}">
    <StackPanel>
        <Label Content="Tox ID: " Margin="10,0" FontWeight="Bold"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding ToxId}" Margin="15,0"/>
        <Label Content="Name: " Margin="10,0" FontWeight="Bold"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Name}" Margin="15,0"/>
        <Label Content="Status message: " Margin="10,0" FontWeight="Bold"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding StatusMessage}" Margin="15,0"/>
        <Label Content="Status: " Margin="10,0" FontWeight="Bold"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding UserStatus}" Margin="15,0"/>
    </StackPanel>
</UserControl>
