<UserControl x:Class="Toxy.Views.FileTransferView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:Toxy.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance ViewModels:FileTransferViewModel}" Height="59" Width="238">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Toxy;component/Resources/Icons.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Toxy;component/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="{StaticResource ToxLightGreyBrush}" CornerRadius="5,0,0,5">
            <Grid>
                <Border Background="White" Margin="10,10,0,10" HorizontalAlignment="Left" Width="40" Height="40"/>
                <TextBlock Text="{Binding Name, Mode=OneWay}" Foreground="{StaticResource ToxMediumGreyBrush}" Margin="55,7,35,0" Height="21" VerticalAlignment="Top"/>
                <TextBlock FontSize="10" Text="{Binding Size, Mode=OneWay}" Foreground="{StaticResource ToxMediumGreyBrush}" Margin="55,0,35,21" Height="14" VerticalAlignment="Bottom"/>
                <TextBlock FontSize="10" Text="{Binding Speed, Mode=OneWay}" Foreground="{StaticResource ToxMediumGreyBrush}" Margin="106,0,62,21" Height="14" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
                <TextBlock FontSize="10" Text="01:37" Foreground="{StaticResource ToxMediumGreyBrush}" Margin="0,0,10,21" Height="14" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="29"/>
                <ProgressBar Margin="55,0,10,10" Height="11" VerticalAlignment="Bottom" Value="{Binding Progress, Mode=OneWay}" />
            </Grid>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Style="{StaticResource ToxGreenButtonTopRightStyle}" Margin="2 0 0 1">
                <Button.Content>
                    <Rectangle Width="10.086" Height="10.087" Fill="{StaticResource ToxWhiteBrush}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource tox_no}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button.Content>
            </Button>
            <Button Grid.Row="1" Style="{StaticResource ToxGreenButtonBottomRightStyle}" Margin="2 1 0 0">
                <Button.Content>
                    <Rectangle Width="8.4" Height="9.4" Fill="{StaticResource ToxWhiteBrush}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource tox_pause}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button.Content>
            </Button>
        </Grid>
    </Grid>
</UserControl>
